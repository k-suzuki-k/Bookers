<body class="Box">

	<% if flash[:notice] %>
		<div class="success">
			<%= flash[:notice] %>
		</div>
	<% end %>

	<p> <span class="show-head">Title:</span> <%= @book.title %> </p>
	<p> <span class="show-head">Body:</span> <%= @book.body %> </p>

	<%= link_to 'Edit', edit_book_path(@book) %>
	<span>|</span>
	<%= link_to 'Back', books_path %>

</body>
